apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: whoami-ingress
  namespace: default
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: whoami
          servicePort: 80
        path: /whoami

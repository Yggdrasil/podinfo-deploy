apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: podinfo-ingress
  namespace: dev
spec:
  rules:
  - host: 'podinfo.heemels.net'
    http:
      paths:
      - backend:
          serviceName: podinfo
          servicePort: 9898